/** Max rows loaded per Failure analytics panel (tables, heatmap tests, charts). */
export const PR_E2E_ANALYTICS_MAX_ROWS = 50;

/** Max rows in the Flakiness tab stability table. */
export const PR_E2E_STABILITY_TABLE_MAX_ROWS = 50;

/** Max runs in the overview Recent runs panel (range-filtered). */
export const PR_E2E_RECENT_RUNS_MAX_ROWS = 50;
